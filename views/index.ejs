<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>XSwitches</title>
  <style>
    body {
      font-family: 'Arial', sans-serif;
      margin: 0;
      background: #1a1a1a;
      color: #fff;
    }
    h1 {
      text-align: center;
      margin-top: 20px 0;
      font-size: 2.5rem;
      color: #f54291;
    }
    h3 {
      text-align: center;
      margin-top: 5px;
      margin-bottom: 20px 0;
      color: #f54291;
    }
    .grid {
      display: grid;
      grid-template-columns: repeat(5, 1fr);
      gap: 15px;
      padding: 20px;
    }
    .box {
      display: flex;
      flex-direction: column;
      background: #2c2c2c;
      border: 2px solid #f54291;
      border-radius: 8px;
      padding: 15px;
      text-align: center;
      justify-content: space-between;
      height: 100%;
      transition: transform 0.3s ease;
    }
    .box:hover {
      transform: scale(1.05);
    }
    /* Image container with 1:1 aspect ratio */
    .image-container {
      width: 100%;
      height: 0;
      padding-top: 100%; /* 1:1 aspect ratio */
      background-color: white;
      position: relative;
      margin-bottom: 10px;
      display: flex;
      justify-content: center; /* Center image horizontally */
      align-items: center;    /* Center image vertically */
    }
    .image-container img {
      max-width: 100%;
      max-height: 100%;
      object-fit: contain; /* Maintain aspect ratio */
      position: absolute;
      display: flex;
      justify-content: center;
      align-items: center;
      top: 0;
    }
    .model {
      font-size: 1.2rem;
      font-weight: bold;
      color: #f54291;
      margin-top: 10px;
    }
    .stars {
      color: gold;
      font-size: 1rem;
      margin: 5px 0;
    }
    .details {
      font-size: 0.9rem;
      color: #ddd;
    }
  </style>
</head>
<body>
  <h1>XSwitches</h1>
  <h3>Where all your switch fantasies come true! </h3>
  <div class="grid">
    <% Object.keys(switches).forEach(key => { %>
      <div class="box">
        <div class="image-container">
          <img src="/images/<%= switches[key].image %>.jpg" alt="<%= switches[key].Model %>">
        </div>
        <div class="model"><%= switches[key].Model %></div>
        <div class="stars">
          <% for (let i = 0; i < 10; i++) { %>
            <% if (i < switches[key].Stars) { %>★<% } else { %>☆<% } %>
          <% } %>
        </div>
        <div class="details">Age: <%= new Date().getFullYear() - switches[key].Birth %> years</div>
        <div class="details">Sexiness: <%= switches[key].Sexyness %>/10</div>
      </div>
    <% }); %>
  </div>
</body>
</html>
